---
# 00: validate inventory and required vars
# 10: install Java, PostgreSQL, and service prerequisites on the OIDC VM
# 20: install Keycloak from the upstream archive and configure systemd
# 30: bootstrap realm and admin group
# 40: configure the Vault OIDC client
# 90: verify service readiness and exported values
- import_playbook: keycloak/00_preflight.yaml
- import_playbook: keycloak/10_install_runtime.yaml
- import_playbook: keycloak/20_deploy_stack.yaml
- import_playbook: keycloak/30_bootstrap_realm.yaml
- import_playbook: keycloak/40_configure_vault_client.yaml
- import_playbook: keycloak/90_verify.yaml
